services:
  syncthing:
    image: syncthing/syncthing:latest
    container_name: syncthing
    hostname: asmodeus-syncthing
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
    volumes:
      - ${VAULT_PATH}:/var/syncthing/vault
      - ${ARCANE}/obsidian/.data:/var/syncthing/config
    networks:
      - asmodeus_default

networks:
  asmodeus_default:
    name: asmodeus_default
    external: true
