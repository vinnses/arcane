services:
  taildns:
    image: tailscale/tailscale:latest
    container_name: lilith-taildns
    hostname: lilith-taildns
    cap_add:
      - net_admin
    devices:
      - /dev/net/tun:/dev/net/tun
    env_file:
      - .env
    volumes:
      - taildns-state:/var/lib/tailscale
      - dns-data:/data
      - ${ARCANE}/taildns/generate-config.sh:/usr/local/bin/generate-config.sh:ro
    command: >
      sh -c "
        /usr/local/bin/containerboot &
        until tailscale status > /dev/null 2>&1; do sleep 1; done;
        generate-config.sh;
        wait
      "
    healthcheck:
      test: ["CMD", "tailscale", "status"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 30s
    restart: unless-stopped

  dnsmasq:
    image: alpine:3.20
    container_name: lilith-dnsmasq
    network_mode: "service:taildns"
    depends_on:
      taildns:
        condition: service_healthy
    volumes:
      - ${ARCANE}/taildns/dnsmasq.conf:/etc/dnsmasq.conf:ro
      - dns-data:/etc/dnsmasq.d/generated/:ro
      - /run/NetworkManager/resolv.conf:/upstream/nm-resolv.conf:ro
    command: >
      sh -c "apk add --no-cache dnsmasq && dnsmasq --keep-in-foreground"
    restart: unless-stopped

volumes:
  taildns-state:
  dns-data:
