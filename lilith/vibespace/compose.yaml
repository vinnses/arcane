services:
  vibecode:
    image: node:20-bookworm
    #container_name: vibing
    hostname: vibration
    restart: unless-stopped
    env_file:
      - .env
    #entrypoint: ["bash", "-c", "set -e; for s in ~/init.d/*.sh; do bash \"$$s\"; done; echo '>>> [vibe] Ready.'; exec sleep infinity"]
    entrypoint: ["sleep", "infinity"]
    working_dir: /home/viber/projects
    stdin_open: true
    tty: true

    volumes:
      # Init scripts — read-write (agents can generate new ones)
      - ${ARCANE}/vibespace/init.d:/home/viber/init.d

      # Dotfiles — mapped directly into home, editable
      - ${ARCANE}/vibespace/dotfiles/.bashrc:/home/viber/.bashrc
      - ${ARCANE}/vibespace/dotfiles/.bashrc.d:/home/viber/.bashrc.d
      - ${ARCANE}/vibespace/dotfiles/.gitconfig:/home/viber/.gitconfig

      # Skills — versionado com arcane
      - ${ARCANE}/vibespace/skills:/home/viber/skills

      # Projects — host bind mount
      - ${PROJECT_PATH}:/home/viber/projects

      # Agent configs (.claude/, etc.) — persists outside named volume
      - ${ARCANE}/vibespace/.data/agents:/home/viber/.agents

      # SSH keys — persists outside named volume
      - ${ARCANE}/vibespace/.data/ssh:/home/viber/.ssh

      # Persistent home (catch-all for everything else)
      - vibestorage:/home/viber

    environment:
      - USER=viber
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}

    ports:
      - "${SSH_PORT:-2222}:22"

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

    networks:
      - lilith_default

volumes:
  vibestorage:

networks:
  lilith_default:
    name: lilith_default
    external: true
